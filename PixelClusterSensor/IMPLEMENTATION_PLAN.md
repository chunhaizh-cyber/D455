# 像素簇视觉外设实施阶段

日期：2026-09-08；2026-09-09 更新实机供包验证。范围只涉及 `D:\D455` 新子工程，不授予其它数字生命仓库的接口变更或接线权限。

## 目标

原始RGBD -> 可追溯像素簇、轮廓、颜色、观测深度和独立补全层 -> 自我合法读取与存在组织。第一阶段不做体素、世界身份、长期记忆和方法自动晋级。

## 分阶段状态

| 阶段 | 目标 | 当前状态 | 退出依据与边界 |
| --- | --- | --- | --- |
| P0 独立工程和格式 | MSBuild工程、控制请求与观察包格式、资源界限 | 已实现 | 新子工程Debug/Release构建；未改原主程序算法 |
| P1 单帧计算与读回 | 标定配准、区域候选、轮廓、颜色引导补全、完整写出和独立读取 | 合成测试及30份实机包读回通过 | 完整数组重复一致、重建一致、几何反例、原始深度不变；不证明真实分割质量 |
| P2 基础控制闭环 | 会话、配置读回、单帧、连续观察、结果拉取、取消、期限与背压 | 合成回放通过，实机生命周期烟测通过 | 实机明确拒绝完整模式源缺口；停止、读完、重开和EOF释放通过；不证明实际SDK断连恢复 |
| P3 真实相机验证 | 打开、逐流读取、设备标定、真实场景包、停止释放 | 有界实机供包烟测通过 | 6次会话、30包；修复启动配对失败，20帧实时优先完成。未验证USB物理拔插、长时采集或曝光稳定 |
| P3.1 启动原始帧诊断 | 原始RGBD、逐流元数据、独立窗口变化判据 | 诊断已验证，联合稳定门禁未通过 | 三次各120帧；每次有1次启动旧彩图拒绝。后段仍受深度波动门槛拒绝，不升级预热状态 |
| P3.2 时域估计探针 | 不重叠时间块、公平像素集合、估计/原始分层、样本年龄及运动反例 | 离线及同类现场新采复验完成，不晋级 | 平均可降低条件化样本波动，但慢移反例仍产生滞后；不能接入默认供包 |
| P3.3 静态累积探针 | 当前修正账本、短时历史补缺、状态/来源/年龄、变化失效 | 两组真实回放及来源读回完成，不晋级 | 缺测状态闪烁及Python探针开销下降，数值噪声不变；同色缺深度运动仍有历史滞留反例 |
| P3.4 旧动态累积验证 | 慢移/遮挡/车载回放、时间证据分层、复用与重置诊断 | 测试完成，动态收益未成立 | 420帧没有历史复用，频繁整帧重置；零残留不是动态复用安全证明 |
| P4 实际分割质量门禁 | 静态、斜面、接触物、多色物、缺测和背景开口的独立真值对照 | 未执行 | 检查误合并、误分裂、边界偏移、错误补全及跨帧波动；先冻结参数选择集和验收集 |
| P5 可选外设控制 | 成像参数读写、动态规格、关注区域、IMU、录制与回放适配 | 未实现 | 每项必须先有能力查询、实际读回和失败路径；当前明确返回unsupported_command |
| P6 自我正式接收 | 真实消费者读取完整材料、组织存在并经正式入口采用 | 未接线 | 必须确认受端DTO、材料所有者、接纳接口、线程及生命周期合同，不新建旁路事实仓 |

## 下一步最小验证

最小实机链路已完成，见 [2026-09-09记录](../docs/codex_analysis/pixel_cluster_sensor_camera_20260909.md)。原始输入、设备标定和逐流时间保存在 `.codex_tmp/PixelClusterSensor/camera_20260909_final/`，不提交原始图片，不直接跑旧60条参数矩阵。

原始窗口诊断已完成，见 [启动验证](../docs/codex_analysis/pixel_cluster_sensor_startup_20260909.md)。本轮场景与前轮不同，不能直接比较缺测率作为优化收益。360帧联合门禁未通过；0.3-1m、1-2m、2-3.5m的后段帧间深度差p95约7/36-37/72-73mm，深度波动有距离差异。逐帧曝光/增益/曝光时间未提供，故不把颜色低变化、固定等待或读取到传感器设置值当作曝光稳定证据。

不重叠块时域估计已经用旧360帧及新采120帧复验，见 [估计层报告](../docs/codex_analysis/pixel_cluster_sensor_temporal_20260909.md)。新片段3/5/9帧平均将共同有效像素对照差p95从52mm降到29.7/23.4/17.4mm，最旧样本相应约66.7/133.4/266.7ms。这不是物理精度或真实边缘保持的验收；全图只有约65.8%像素进入条件化比较，边界缺测仍未覆盖。

2026-09-09 根据用户效率方向改试静态累积账本，不继续扩大平均窗口。当前深度优先写入，仅0缺测允许有限年龄历史候选；13组反例、Release/Debug回放及20份真实输出来源读回通过，详见 [静态累积报告](../docs/codex_analysis/pixel_cluster_sensor_accumulation_20260909.md)。这不是“所有历史永久有效”，也不是完整存在/轮廓记忆。

旧动态片段测试已经完成，见 [动态累积报告](../docs/codex_analysis/pixel_cluster_sensor_dynamic_accumulation_20260909.md)：全局颜色门禁几乎每帧重置，未产生历史候选；不是动态复用机制通过。旧室内数据时间缺口不能补造，车载数值时间戳也缺时间域，均只用于有明确限制的诊断。

下一步先区分局部变化与相机运动：局部变化仅局部失效，相机运动需要同簇关联/重投影，不可靠时保持降级。再用独立动态边界/对象证据确认“历史表面还占据当前像素”，测试错误滞留、失效及时性与源年龄，而不是单纯放宽全图门槛。通过后再讨论原始深度到配准像面的合同及C++影子接入。当前正常供包仍不启用累积/时域滤波，不改变50ms配对门槛或原始数值，不晋级best。

实时可用性、物理深度准确性、目标身份连续性和数据供包分别验收。处理速度本轮仅记录，不宣称优于主工程，也不修改既有100ms评分门槛或任何best配置。
