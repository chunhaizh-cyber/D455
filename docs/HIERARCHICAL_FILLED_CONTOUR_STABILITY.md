# 全填充轮廓正方形分级压缩与比较

## 目标

该诊断用于验证以下静态轮廓表示：

1. 对闭合外轮廓采用内部全部填充，不保留内部深度孔洞、确认穿孔或临时黑洞。
2. 消除轮廓在原画面中的位置，只保留外剪影形状。
3. 把轮廓放入边长为8的正整数倍的正方形。
4. 分级压缩并逐级比较，最小为8x8。

该方法只测试外剪影稳定性。真实能看到背景的穿孔仍属于最终像素归属事实，不能因为本诊断填洞而在生产输出中删除。

## 处理流程

```text
.b8x8填充轮廓
→ 从外部洪泛背景
→ 把所有封闭黑洞填为前景
→ 重新裁剪外轮廓bbox
→ 居中放入下一2次幂正方形
→ 2x2面积池化逐级压缩
→ 每级生成二值特征和占用特征
→ 同ID连续帧逐级比较
```

正方形边长选择能容纳该轮廓全部样本的最小2次幂，因此天然是8的正整数倍。本轮主轮廓最大尺寸小于512，使用：

```text
512 → 256 → 128 → 64 → 32 → 16 → 8
```

轮廓不会在基础512级被拉伸；只做居中补黑。后续每级由连续2x2区域的平均占用率生成。

## 两类特征

### 二值特征

每格占用率 `>=50%` 记为前景，否则记为背景，再按行优先逐位压缩。

- 512级：32768 bytes
- 256级：8192 bytes
- 128级：2048 bytes
- 64级：512 bytes
- 32级：128 bytes
- 16级：32 bytes
- 8级：8 bytes

二值特征用于计算前景IoU和精确重复值。8x8相对512x512按位载荷压缩4096:1。

### 占用特征

每格保留0-255量化占用率，用于计算：

```text
占用相似度 = 100 * (1 - 所有格的平均绝对占用差)
```

占用特征可以揭示二值阈值后被隐藏的小幅面积变化。二值值相同不代表占用值相同。

## 运行

依赖 Python `numpy` 和 `Pillow`：

```powershell
python .\scripts\analyze_hierarchical_filled_contour_stability.py `
  --input-dir=analysis_runs\binary_contour_static_long_repeat_001 `
  --input-dir=analysis_runs\binary_contour_static_long_repeat_recheck_001 `
  --output-dir=analysis_runs\hierarchical_filled_contour_long_001 `
  --contour-id=1 `
  --min-side=8
```

输出：

```text
hierarchical_feature_hashes.csv
hierarchical_pair_metrics.csv
hierarchical_filled_contour_summary.json
hierarchical_filled_contour_report.md
```

## 长时测试结果

输入为两段锁定相机控制、相同静态场景的回放；每段预热后570帧，共1140帧、1138个段内连续帧对。测试对象是两段中均持续存在且平均填充IoU最高的 `contour_id=1`。

| 边长 | 二值IoU p05 / p50 / p95 | 二值IoU >99% | 二值不同值 | 重复帧 | 相邻精确重复率 | 最大重复次数 |
|---:|---:|---:|---:|---:|---:|---:|
| 512 | 96.5192% / 98.5003% / 99.4309% | 22.4956% | 1140 | 0 | 0% | 1 |
| 256 | 96.6406% / 98.7959% / 99.5028% | 33.7434% | 1140 | 0 | 0% | 1 |
| 128 | 95.5418% / 97.7523% / 99.5105% | 14.3234% | 1140 | 0 | 0% | 1 |
| 64 | 96.8212% / 98.8408% / 99.5709% | 37.9613% | 1136 | 4 | 0% | 2 |
| 32 | 97.0443% / 98.7685% / 99.7531% | 42.3550% | 320 | 820 | 2.1968% | 90 |
| 16 | 97.1154% / 99.0385% / 100% | 52.1968% | 32 | 1108 | 21.0018% | 382 |
| 8 | 100% / 100% / 100% | 99.4728% | 2 | 1138 | 99.4728% | 1137 |

首次二值重复出现于：

- 64级：第一段 frame 507 与 frame 542；
- 32级：第一段 frame 34 与 frame 41；
- 16级：第一段 frame 31 与 frame 32；
- 8级：第一段 frame 30 与 frame 31。

所有级别的0-255占用特征仍然是1140帧1140种值，占用相似度p50约99.40%-99.45%。这说明原始轮廓的连续面积差异没有消失，只是在粗二值阈值后归并为少量离散形状。

128级二值IoU没有单调优于256级，是50%阈值附近的格子发生翻转造成的量化混叠；因此不能假设分辨率越低，所有统计指标都会单调改善。

## 当前判断

- 512-128级仍保留几乎全部高维变化，没有精确重复。
- 64级开始出现少量重复，但不足以作为稳定模板。
- 32级已经显著归并形状，可用于中等粒度候选比较。
- 16级在信息量和稳定性之间形成明显过渡。
- 8级适合极粗的快速变化扫描，但只有2种二值值，不能承担精细轮廓确认。
- 生产使用时应先用8/16级快速比较，再在变化触发后回到32/64级或原始轮廓复核，不能只保留8x8结果。
